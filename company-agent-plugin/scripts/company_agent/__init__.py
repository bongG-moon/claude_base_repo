"""Company Agent harness runtime.

The package intentionally depends only on the Python standard library so that
the plugin can run on offline Windows PCs without a package installation step.
"""

__version__ = "1.4.22"
